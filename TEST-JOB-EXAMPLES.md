# 🧪 Test Job Examples

## Quick Test Job (Simple)

**Title:**
```
Test Training Job
```

**Description:**
```
Testing the decentralized AI training network with a simple neural network model.
```

**Config JSON:**
```json
{
  "epochs": 10,
  "batchSize": 32,
  "learningRate": 0.001,
  "optimizer": "adam"
}
```

---

## Image Classification Job

**Title:**
```
Image Classification Model Training
```

**Description:**
```
Training a CNN model for image classification using CIFAR-10 dataset.
```

**Config JSON:**
```json
{
  "epochs": 20,
  "batchSize": 64,
  "learningRate": 0.0001,
  "modelType": "CNN",
  "dataset": "CIFAR-10",
  "optimizer": "adam",
  "lossFunction": "categorical_crossentropy"
}
```

---

## Language Model Training

**Title:**
```
Transformer Language Model Training
```

**Description:**
```
Training a transformer-based language model for text generation.
```

**Config JSON:**
```json
{
  "epochs": 10,
  "batchSize": 32,
  "learningRate": 0.001,
  "modelType": "transformer",
  "vocabSize": 50000,
  "hiddenSize": 768,
  "numLayers": 12,
  "optimizer": "adamw"
}
```

---

## Reinforcement Learning

**Title:**
```
DQN Reinforcement Learning Agent
```

**Description:**
```
Training a Deep Q-Network agent for game playing.
```

**Config JSON:**
```json
{
  "epochs": 100,
  "batchSize": 128,
  "learningRate": 0.0003,
  "modelType": "DQN",
  "environment": "CartPole",
  "gamma": 0.99,
  "epsilon": 0.1
}
```

---

## Computer Vision Model

**Title:**
```
ResNet Image Recognition Model
```

**Description:**
```
Training a ResNet model for image recognition tasks.
```

**Config JSON:**
```json
{
  "epochs": 15,
  "batchSize": 32,
  "learningRate": 0.0001,
  "modelType": "ResNet",
  "depth": 50,
  "dataset": "ImageNet",
  "optimizer": "sgd",
  "momentum": 0.9
}
```

---

## Natural Language Processing

**Title:**
```
BERT Text Classification Model
```

**Description:**
```
Fine-tuning BERT for text classification on custom dataset.
```

**Config JSON:**
```json
{
  "epochs": 5,
  "batchSize": 16,
  "learningRate": 2e-5,
  "modelType": "BERT",
  "maxLength": 512,
  "numLabels": 3,
  "optimizer": "adamw"
}
```

---

## Quick Copy-Paste (Simplest Test)

**Title:**
```
Quick Test Job
```

**Description:**
```
Quick test to verify job processing works.
```

**Config JSON:**
```json
{"epochs": 10, "batchSize": 32, "learningRate": 0.001}
```

